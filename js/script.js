(() => {const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];const menu=q('.menu'),links=q('.links');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));links.classList.toggle('open',!open)});qa('.filters button').forEach(b=>b.addEventListener('click',()=>{qa('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');qa('.stay').forEach(c=>c.hidden=b.dataset.filter!=='all'&&!c.classList.contains(b.dataset.filter))}));const map=q('#active-map'),dialog=q('#lightbox');qa('.map-tabs button').forEach(b=>b.addEventListener('click',()=>{qa('.map-tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');map.src=`assets/images/${b.dataset.map}.jpg`;map.alt=`${b.textContent} resort map`}));q('[data-lightbox]').addEventListener('click',()=>{dialog.querySelector('img').src=map.src;dialog.showModal()});dialog.querySelector('button').addEventListener('click',()=>dialog.close());})();
